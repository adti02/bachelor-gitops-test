apiVersion: apps/v1
kind: Deployment
metadata:
  name: meine-test-app
  namespace: default
spec:
  replicas: 5 #2
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
      - name: nginx
        image: nginx:latest
